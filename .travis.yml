
language: php

php:
  - 5.5
  - 5.4
  - 5.3

before_script:
  - composer self-update
  - composer install
  - mysql -e 'create database test_3;'
  - echo "USE mysql;\nUPDATE user SET password=PASSWORD('password') WHERE user='root';\nFLUSH PRIVILEGES;\n" | mysql -u root

script:
  - phpunit
  - bin/behat
  - bin/phpspec run